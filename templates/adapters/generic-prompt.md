# WorktreeProof generic prompt

Inspect first. Propose relative, non-overlapping scopes and a dry-run plan.
Never capture secrets or auth material; do not overwrite, traverse outside the
repository, follow symlink escapes, install software, or call a network.
