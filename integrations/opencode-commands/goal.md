---
description: Set one immutable WorktreeProof goal with terminal acceptance gates
agent: plan
---
Use the WorktreeProof `goal_set` tool to create exactly one concrete goal from the user's request below. Record the outcome, terminal acceptance criteria, allowed scope, authoritative baseline, and original deadline (or `none`). Do not invent a deadline or broaden the contract. Then show the fixed goal and ask no unnecessary questions.

User goal: $ARGUMENTS
