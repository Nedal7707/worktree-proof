---
description: Create a fixed WorktreeProof plan from the current goal
agent: plan
---
Use the WorktreeProof `plan_create` tool to create a non-overlapping task plan for the current goal. Every task must have a named terminal acceptance condition and a concrete scope. Do not count plans, branches, commits, tests without named acceptance, or commentary as terminal progress. Show the resulting terminal denominator.

Additional planning direction: $ARGUMENTS
