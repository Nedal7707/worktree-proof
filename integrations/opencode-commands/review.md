---
description: Run the WorktreeProof terminal review gate and summary
agent: plan
---
Use `review_gate` and `review_summary`. Report each fixed ledger separately as terminalClosed/terminalTotal with named evidence. Use FORECAST_UNAVAILABLE unless the owner explicitly requested a forecast and the evidence contract permits one.

Review focus: $ARGUMENTS
